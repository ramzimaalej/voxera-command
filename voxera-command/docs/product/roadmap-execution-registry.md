---
title: Roadmap Execution Registry
version: 19
status: active
updated: 2026-07-28
owner: you
---

> **What this is.** The executable companion to [`roadmap.md`](./roadmap.md): one ordered table mapping every roadmap item to the **exact babysitter workflow** that implements it, plus the selection algorithm the [`resume-roadmap`](../../.claude/skills/resume-roadmap/SKILL.md) skill follows to pick the next move. `roadmap.md` says *what/why/when*; this registry says *which command, from which repo, in what order*.
>
> **Confidentiality (ADR-0014/0015).** This registry is exec-only and may name confidential bets. Engineering processes never read it — when a workflow runs, the orchestrator (you / Claude) reads this registry here, then passes a **self-contained `featureDescription`/`spec` down** into the code repo's process. The engineering process never reads up into `voxera-command`.

## How "resume" selects the next move

1. **Read state** — this registry + [`roadmap.md`](./roadmap.md) + [`gap-analysis-crm-to-product.md`](./gap-analysis-crm-to-product.md), and for any item with a feature folder, that folder's `status.json` (CRM) or `spec.md` frontmatter (sites).
2. **Pick the next executable item** — the highest-priority item that is **not `DONE`** and whose **every dependency is `DONE`**:
   - Horizon order: **Now → Next → Later**.
   - Within a horizon: lower **Order** first; an item already `IN-SDLC` outranks a `NOT-STARTED` one (finish in-flight work before opening new).
   - The two **tracks run in parallel** — the CRM **product spine** (B-bets) and the website **motion** are independent; "next" may legitimately offer one of each.
3. **Map to a command** — look up the item's **Workflow** template below and fill its args from the bet text + gap ids.
4. **Propose, then run** — state the one command it will run + a one-line rationale, get a go-ahead, then `cd` into the repo and invoke. (The babysitter run carries its own human gates; CRM writes no production code before `PLAN_APPROVED`.)
5. **On completion** — tick the `roadmap.md` checkbox, set this item's **Status** to `DONE`, and run `HARVEST`. Then the next `resume` advances to the newly-unblocked item.

## Status vocabulary

`NOT-STARTED` (no spec) · `SPEC-DRAFT` (spec exists, unapproved) · `IN-SDLC:<phase>` (feature folder live; phase from `status.json`) · `BLOCKED` (a dependency isn't `DONE`) · `DONE` (merged + harvested). `BLOCKED` is derived, not stored — recompute it from dependencies each run.

## Workflow templates

Fill `<…>` from the registry row + the bet's `roadmap.md` line and its `GAP-xx` ids in the gap-analysis dossier.

| Template | Command |
|---|---|
| **CRM-BUILD** *(fresh spine bet, no feature folder yet)* | `cd voxera-crm && /babysitter:call --process .a5c/processes/build-feature.process.js#process --inputs '{"featureDescription":"<bet objective composed from the roadmap line + its GAP-xx>","repoRoot":"."}'` — the gate-aware process drives the whole SDLC (spec → design-review → tests → plan → implement → quality gate), honoring `status.json` (no production code before `PLAN_APPROVED`). |
| **CRM-RESUME** *(feature folder exists, mid-SDLC)* | `cd voxera-crm`, read `features/<id>/status.json`, run the SDLC skill for the phase, each gated by `approve-feature-phase`: `DRAFT`→`/refine-feature-spec` · `SPEC_APPROVED`→`/run-design-review` · `DESIGN_APPROVED`→`/generate-test-cases` · `TESTS_APPROVED`→`/generate-implementation-plan` · `PLAN_APPROVED`→`/implement-approved-feature` · `VERIFYING`→`/verify-feature` → `DONE`. |
| **WEB-BUILD** *(site item with an approved spec)* | `cd voxera-website && /babysitter:call --process ../voxera-os/.a5c/processes/build-feature.js#process -- spec features/<id>/spec.md` |
| **WEB-DESIGN** *(site item with no spec yet)* | `cd voxera-website && /babysitter:call --process ../voxera-os/.a5c/processes/design-feature.js#process -- request "<item intent>"` → produces a Software Delivery Intake / spec, then **WEB-BUILD**. |
| **CRM-FIX** | `cd voxera-crm && /babysitter:call --process .a5c/processes/fix-bug.process.js#process --inputs '{"bugDescription":"<…>","repoRoot":"."}'` |
| **CRM-DEBT** | `cd voxera-crm && /babysitter:call --process .a5c/processes/pay-down-debt.process.js#process --inputs '{"scopeHint":"<TD-/PP- id or path>","repoRoot":"."}'` |
| **HARVEST** *(on DONE)* | `cd <repo> && /babysitter:call --process ../voxera-os/.a5c/processes/harvest-feature.js#process -- feature features/<id>` |

> CRM uses its **local gate-aware** processes (canonical for that repo); sites use the **voxera-os baseline** processes by relative path. Never copy a process — invoke it in place.

## Track A — CRM product spine (`voxera-crm`)

Sequenced by dependency (the keystone is B1; compliance gate B6 precedes first live call B5). Bet text + `GAP-xx` live in [`roadmap.md`](./roadmap.md) / [gap-analysis](./gap-analysis-crm-to-product.md).

| Order | Bet | Horizon | Depends on | Status | Workflow | Feature folder |
|---|---|---|---|---|---|---|
| 1 | **B0** Robust authorization layer | Now | — | `DONE` (2026-06-29) | ✅ verified + **harvested 2026-06-30** (LSN-021..025, TD-057/058, ADR-0024 accepted, pattern doc folded) | `features/_archive/20260624-robust-authz-layer` |
| 2 | **B1** Agent data-model spine (Context + Decisions + cost) | Now | B0 | `DONE` (foundational 2026-07-03; **Context + cost 2026-07-07**) | ✅ full SDLC. Foundational: design-review 5 lenses → 39 TCs → 8 slices → verify DONE; ADR-0028/0029. **TD-086 (slices 4-5) now DONE** (2026-07-07): BusinessContext (AC-5) + AgentActionCost (AC-6) — 7-lens design pass → ~48 TCs → 5 slices test-first (S-A schema/migration → S-E adapters + 5 P15 runtime exceptions; commits 84fe45a7..1ee4db01) → verify DONE (core/agent 381 tests green, gate:drift + gate:ratchet PASS). Un-stubs B3's `BusinessContextPort`/`AgentCostLedgerPort`. **Only AC-7 (inspect chip) still deferred** → B7 (frontend, ADR-0034 Relay). Spawned TD-101/TD-102/OQ-TDD-5 | `voxera-crm/features/20260701-agent-data-model-spine` |
| 3 | **B2** Outbox fully wired + agent-event emission | Now | B1, B0, TD-009 | `DONE` (2026-07-04) | ✅ verified + **harvested** (Kafka-style consumer groups over a RANGE-partitioned Postgres outbox; ADR-0030/0031/0032, LSN-035..039, pattern doc `outbox-consumer-group-relay.md`). TD-009 emit **taxonomy** settled (ADR-0032 + event catalog); per-context emit wiring = mechanical follow-on | `voxera-crm/features/_archive/20260703-outbox-relay` |
| 4 | **B3** Brain + fixed agent roster + base | Next | B1 | `IN-SDLC:IMPLEMENTING` (opened 2026-07-05) | **Runtime substrate DONE 2026-07-06** (design-first, adversarially reviewed 2×): Mastra runtime + `AgentRuntimeService` turn loop over the B1 spine, delegating-principal authz (agent ⊆ human, no escape confirmed), Operator role, 3 authz-wrapped tools, TD-100 hardening, wall-clock reaper, **async/BullMQ entrypoint** (the repo's first consumer; propose-then-handoff, never auto-exec). **Grounding + cost adapters now LIVE (TD-086 DONE 2026-07-07)** — the real `BusinessContextPort`/`AgentCostLedgerPort` bind over the B1 Context+cost spine. **OQ-5 approval sink now DONE 2026-07-10** — the propose-then-handoff loop is closed: async ProposedActions are captured as a mutable `ProposedActionApproval`, a human approves/rejects under their RE-DERIVED ability (agent ⊆ human, null→fail-closed), the write executes under the human via the role-free executor (injection defense: the model's args never reach it — only human-confirmed values), two-path durable audit + append-only decisions; full SDLC (6-lens design → 15 slices test-first → 538 tests green, coverage ≥ floor, gate:ratchet PASS). Spawned TD-104 (lazy-expiry never persists — safety intact, tx-structure fix pending). **TD-101 turn-level LLM retry DONE 2026-07-11** (commit `ef30a8ab`) — a provider transient (Anthropic 500/429/timeout) no longer terminally fails an automation run: `runChatTurn` retries a retryable `generate()` (cap 2, jittered, budget-aware) before the unchanged `LLM_FAILURE` fallthrough; ai-agent design → adversarial review → test-first, 149 runtime tests green; spawned TD-105 (circuit-breaker follow-up, low). REMAINING: the other 6 roles + copilot surface | `voxera-crm/features/20260707-agent-approval-sink` (OQ-5, DONE); substrate `voxera-crm/features/_archive/B3-agent-runtime` |
| 5 | **B4** Typed Workflow DSL + primitives + write-path | Next | B1, B3 | `DONE` (2026-07-14 — all 4 GAPs across slices 1-3) | **Slice 1 DONE** — the executable typed substrate: full build-feature SDLC (spec → 5-lens design-review DDD/ERD/Prisma/OO/TDD all converged → tests → plan → implement). Replaces the untyped `Pipeline.definition Json` (written-but-never-parsed; the real path branched hardcoded stage arrays on a literal account id) with **normalized `Workflow`+`Stage` models** (OQ-1=Option B), a typed `linear_funnel` define/version write-path (CASL, `Workflow.version` optimistic lock, full-set-replace), a validated stage-transition write-path (single-source `Lead→LeadPipeline(isActive)→Workflow` resolve, fail-closed; atomic `Lead.stage`+`PipelineEvent` in one tx via the resource-owned `LeadStageDenormPort` seam — context-isolation held 67), and a one-member primitives registry. 62 core/pipeline + 70 account + 75 authz tests green; gate:ratchet + gate:drift PASS. Legacy `LeadWorkflowServiceImpl` parallel-served (OQ-2, IRON-LAW) → TD-106. **Slice 2 (agent-driven transitions) DONE 2026-07-13** — a `transition_lead` Operator tool: the agent PROPOSES a stage move, a human APPROVES it, driving slice-1's `StageTransitionService` under the human via B3 OQ-5's approval sink (an integration of two locked designs, no new subsystem). 3-lens design (ai-agent/OO/TDD; ddd/erd/prisma/graphql scoped out — no schema change, `requiresPrismaMigration=false`); 6 slices test-first; **full backend:test 224 suites / 1920 tests green, gate:ratchet + gate:drift PASS, core→app=0, no new app/ file**. Injection-contained (the human-confirmed move executes, never the model's preview), agent⊆human, execute-once (tx-join, no migration), durable-deny-on-denial-only — all test-confirmed. Surfaced + fixed a pre-existing OQ-5/slice-1 SDL drift; spawned TD-108 (SDL fast-gate) + TD-109 (existsUnderHuman extract); LSN-050/051. **Slice 3 (time-triggers, GAP-34) DONE 2026-07-14** — the first UNATTENDED driver of the slice-1 transition substrate: a stage declares a `StageTimeTrigger` (`afterMs` → `transition_to` a terminal stage); on a transition INTO the stage the engine materializes a `ScheduledTransition` job in the SAME tx, cancels it on exit, and an `@Interval` poller (Design A tx-join, version-guarded claim) fires due jobs through the slice-1 `PrimitiveRegistry` under a **per-account System Automation Principal** ([ADR-0036](../../../voxera-crm/decisions/ADR-0036-system-automation-principal.md)) — so the human-less fire is CASL-real (authorized, tenant-scoped, audit-attributed), never bypassed. Full SDLC (6-lens design → 27 TCs → 8 slices A-H test-first). 2 new models (additive/55P04-safe migration, human-approved) + FK `stageTimeTriggerId` **ON DELETE SET NULL** (Correction 2 — Restrict was status-blind) + a raw partial-unique dedupe. ~1826 tests across all zones green, 0 failures; `gate:ratchet`+`gate:drift`+`no-sdl-change` PASS (`core→app` 0, `context-isolation` 67, `app-tree` 422→424 reviewed +2 for the iam adapter+helper). Both dossier corrections proven (separate-tx-settle-on-throw LSN-048; SetNull re-version). **REMAINING B4 slices:** forms/reports/custom-objects/non-linear-kinds. | `voxera-crm/features/20260711-workflow-dsl-linear-funnel` (slice 1) · `20260713-agent-driven-transitions` (slice 2) · `20260713-workflow-time-triggers` (slice 3, DONE) |
| 6 | **B6** Compliance gate engine + per-motion frames | Next | B4 ✓, **TD-081 ✓** | `DONE` (2026-07-17) | ✅ full SDLC → 6-lens design → 7 slices A-G test-first. New `core/compliance` context [ADR-0038] binds the `CallDispatchPolicyToken` → `ComplianceCallDispatchPolicy` (calling-hours/DNC/consent + recording-consent carry [ADR-0039], per-motion config-as-code presets floor-clamped); deny-as-data, fail-closed, PII-safe audit; zero `CallDispatchService` change (AC-1 diff-proven); fail-OPEN closed structurally. ~1377 tests by-zone, gate:ratchet+drift+no-sdl PASS; 2 additive/55P04-safe migrations. NOT yet harvested | `voxera-crm/features/20260716-compliance-gate-engine` |
| 7 | **B5** AI outbound voice operator + scoped tools | Next | B3, B4, B6 | `CODE-COMPLETE` (verify-pending; 2026-07-18/19) | *The differentiator.* Full SDLC → 9-lens design-review → [ADR-0040](../../../voxera-crm/decisions/ADR-0040-ai-voice-operator-driver.md) (event-driven core/agent driver; every Ultravox tool-call executes SERVER-SIDE under the re-derived delegating human [agent⊆human, structural injection defense]; qualification PROPOSED / disposition COMMITTED). **All 4 slices committed:** S1 placed-call-via-B6-gate + server-originate + migration (`68689966`); S2 Ultravox session + inbound webhooks [Svix-HMAC fail-closed, tenant-from-matched-row, no-PII, version-locked idempotent close, disclosure-before-Connect] (`cb9ce363`); S3 the safety-critical tool surface [VoiceToolCallExecutor agent⊆human null→fail-closed, closed-surface, proposal-inline-not-terminated] (`fc99a536`); S4 DE voice + cost [no-double-count sourceRef] (`1244a338`). gate:ratchet+drift PASS (core→app 0, isolation 67). **⚠️ 4 pglite safety-e2e CI-PENDING** (createTestApp-boot OOM, never assertions) — the ONE gate before verify→DONE. MERGED+PUSHED 2026-07-20. | `voxera-crm/features/20260717-ai-voice-operator` |
| 8 | **B7** Trust & observability surface | Next | B1, B3, B5, B0 | `IN-SDLC:IMPLEMENTING` (code-complete/verify-pending; 2026-07-20/21) | The observability-beats-automation moat made visible; the FIRST major FE product bet (net-new Relay Scenes on the Bfe/ADR-0034 `src/core`). Full 7-lens design-review + [ADR-0041](../../../voxera-crm/decisions/ADR-0041-graphql-subscription-infrastructure-and-live-transcript-transport.md) (first GraphQL subscription infra). **Backend read surfaces DONE** (source-verified): S1 reasoning + the honest-flag mapper [OQ-AI-2: a human reject's Handoff record ≠ agent escalation] (`02367cc6`); S3 proposal + the **reused** approve/reject [decide service verbatim, no field authScopes, structural no-repoint, deny-as-data] (`9f681ee5`); S4 the CausalTimelineComposer [no-double-count, isolation 67] + autonomy (`ca6208a0`). **FE DONE** on Relay `src/core` (temp-schema-validated relay-compile+type:check+tests): S2 reasoning Scene + AC-2 chip + the VOX-FE-009 CoreAbilityContext seam (`515189d0`); S3 proposal diff + approve/reject [caught+fixed a write-path injection/corruption bug] (`c8c21fbc`); S4 causal timeline + autonomy (`3ea3b178`). **Slice 5 (live-call subscription) DEFERRED** — B5-gated + the graphql-ws-install alwaysBreakOn. REMAINING: post-merge wiring (main.tsx/Router.tsx/i18n) + CI e2e → verify. | `voxera-crm/features/20260720-trust-observability-surface` |
| 9 | **B8** Daily-ops shell + report engine + dashboard | Next | B4, B7 | `IN-SDLC:IMPLEMENTING` (code-complete A-G/verify-pending; 2026-07-21..26) | *The surface the operator buys + "trustworthy numbers" made mechanical.* Full 7-lens design-review + [ADR-0042](../../../voxera-crm/decisions/ADR-0042-analytics-deterministic-core-context.md) (new `core/analytics` deterministic core-native context; compute-on-read no read-model; numbers computed via a single-source metric registry, **NEVER LLM** — import-graph-boundary-enforced). **All slices A-G committed:** A engine core [39 pure units, ⭐unpriced≠$0 mapper + ⭐no-LLM boundary] (`667f366e`); B 2 cost groupBy methods [resolved a real tz-determinism bug — double AT-TIME-ZONE] (`9585543e`); C funnel adapter + **PipelineEvent index migration** (REPLACE, user-approved) + OQ-R1 default (`82c0f20c`); D **AnalyticsReport ADD-VALUE migration** (55P04-safe) + seeder (`d1f04e3b`); E GraphQL read surface [3 typed queries + Measure union never-$0, fail-closed; fixed a latent CASL-Subjects type gap] (`f2676bc2`); F+G FE ops-shell + funnel/cost dashboard [Relay src/core, 35/35 tests, MeasureValue never-$0, GAP-48 not-yet-available] (`565c7a0c`). Both migration breakpoints cleared. gate:drift+ratchet PASS throughout. REMAINING: post-merge wiring (app.module AnalyticsModule import + /ops route + i18n) + CI e2e → verify. Live-calls BOARD proper stays B7-Slice-5/B5-gated. | `voxera-crm/features/20260721-daily-ops-shell-report-engine` |
| 10 | **B9** Context Copilot + Configurator + self-serve onboarding | In-progress | B1, B3, B4, **TD-082 ✓** | `IN-SDLC:VERIFYING` (slice-1 6/6 + slice-2/GAP-08 5/5) | **CRM-BUILD.** Prerequisite **TD-082 CODE-COMPLETE** (`voxera-crm/features/20260726-forms-jsonforms-migration`, VERIFYING). **B9 slice 1 (the "config-not-rebuild" derivation core) is DESIGN+PLAN done, BUILD-READY** → `voxera-crm/features/20260727-configurator-workspace-derivation` (spec → **7-lens design-review, all approved** → test-cases → 6-slice plan → PLAN_APPROVED). MAJOR source-verified reframing: `BusinessContext` (`BusinessContextVersion`) + the `Configurator` role slot ALREADY EXIST → mostly WIRING; **NO migration** (config rides the existing `descriptor Json` + `toolName`). OQ-1=HYBRID (a dedicated single-turn **TOOL-LESS** Configurator: LLM proposes a funnel → deterministic normalizer → `WorkflowDefinition.create()` I-1..I-7 DSL-valid gate → Pending B7 proposal → human-approves). Safety BY CONSTRUCTION (empty tool surface + `GROUNDING_PRECEDENCE_RULE` + agent⊆human + propose-not-apply). Reuses B4 write-path / B3 runtime (sibling-driver) / B7 approve lifecycle (single path) / B0. **BUILD COMPLETE — all 6 slices** (on `rm/agentic`): `e9879fd3` pure DSL gate · `e53b8c64` VO+capture (propose-not-apply BY CONSTRUCTION) · `c29a7370` the tool-less Configurator run + INJECTION MATRIX (GREEN on-host — empty tool surface, worst case a Pending never an apply) · `c149d2d4` the apply (agent⊆human TRIPLE-GATE + edit-byte-exact) · `5c18fbec` GraphQL surface + async enqueuer (INV-11, projection-not-column, single approval path; 29 on-host) · `caa3565f` the FE (extend B7 ProposalReviewScene + BusinessContextScene; 37 on-host, B7 single-write byte-stable). **NO migration all 6; core→app 0, isolation 67 held; gate:drift PASS; single approval path.** Backend safety verified on-host (the injection matrix) + by construction (propose-not-apply/apply triple-gate); the FE fully on-host. **DONE gated on the CI e2e run** (the pglite service cases + the GraphQL authZ e2e + `backend:graphql:gen:schema` — `[CI]`-pending on the OOM host, the B5/B8/forms precedent). AC map + DONE-blockers in `voxera-crm/features/20260727-configurator-workspace-derivation/VERIFY-SUMMARY.md`. **SLICE-2/GAP-08 (the adaptive interview UI) BUILD CODE-COMPLETE 2026-07-28** — `voxera-crm/features/20260728-context-copilot-interview`, all 5 slices on `rm/agentic` (a bounded conversational agent interviews the operator → writes the 11-section `BusinessContextVersion` live [replacing slice-1's form] → hands to slice-1's `requestWorkspaceDerivation`): `d20f1967` the pure gate (`ContextDraftParser` fail-closed + `InterviewCoverage` + the single-sourced `isSectionValuePresent` → coverage⟺parse structural agreement) · `3921cf03` the write gate (`complete` = server re-parse [advisory flag] + coverage gate + `appendVersion` under the human; 7/7 on-host) · `affa4b0e` the interview turn (`advanceTurn` + `CONTEXT_COPILOT_CONFIG` empty-tools + `wrapUntrusted` extracted-shared + transcript sanitization + the injection matrix; **advanceTurn NEVER appends** — the LLM is structurally outside the durable write; 53/53 on-host FakeLlm) · `379c42b4` GraphQL (`sendInterviewMessage` capability-gate + `completeInterview` deny-as-data union reusing the setBusinessContext shape; HANDOFF-1 no 2nd derive; 12/12 resolver-unit) · `11f76c68` the FE chat (`ContextInterviewRegion` client-custody `useReducer` + 4 graph-blind Primitives + read-only preview + the REUSED derive + routed `pilot/configurator` + VOX-FE-009 served + a11y; 20 on-host / FE suite 1290). **NO migration all 5; the LLM structurally outside the write; agent⊆human; drift PASS, core→app 0, isolation 67, app-tree 452, jscpd 381, frontend-core-boundary 0 — all baselined.** Every AC (1-6) has on-host evidence (map + [CI]-gate in `features/20260728-context-copilot-interview/handoff.md`); DONE gated on the same [CI] e2e (pglite append-row + wired authZ e2e + `backend:graphql:gen:schema` + `frontend:relay`). Deferred to LATER B9 slices: agents/gates/reports derivation (GAP-09 breadth + GAP-43), presets/templates (GAP-35/37), re-interview/conversational edit. | TBD |
| 11 | **B10** Hybrid-close handoff | Later | B5, B7 | `NOT-STARTED` | **CRM-BUILD** | TBD |
| 12 | **B11** OS object layer + brand expression | Later | B4, B9 | `NOT-STARTED` | **CRM-BUILD** | TBD |
| 13 | **B12** Continuous-improvement loop | Later | B5, B7, B8 | `NOT-STARTED` | **CRM-BUILD** | TBD |

**Next ready on this track:** the **Now horizon is closed** (B0/B1/B2 DONE) and **B4 is now fully DONE (2026-07-14)** — all 4 GAPs across slices 1-3. **B3 stays the open in-flight bet** (runtime substrate + grounding/cost adapters + OQ-5 approval sink + TD-101 retry all DONE; the **6 other agent roles** are gated on downstream consuming bets — Configurator/Context-Copilot ride B9, Critic/Promoter ride B12 — so B3's remainder is effectively downstream-gated). With B4 done, the next spine bets by Order are **B6** (compliance gate, deps B4 ✓) and **B9** (Configurator, deps B1/B3/B4 ✓), but **each is gated on a prerequisite: TD-081** (no backend outbound-dispatch seam — dialing is browser-initiated → the ADR-0023 gate has nothing to front; remediation = a `CallDispatchService` chokepoint in `core/communication`) blocks **B6**, and **TD-082** (ADR-0006 `@jsonforms` vs shipped `@rjsf`) blocks **B9**. **TD-081 is now DONE (2026-07-15)** — the `CallDispatchService` dispatch seam shipped (`voxera-crm/features/20260714-call-dispatch-seam`, [ADR-0037](../../../voxera-crm/decisions/ADR-0037-outbound-call-dispatch-authorization-mechanism.md)), so **B6's precursor is cleared** and its `CallDispatchPolicy` `@Global` bind-point exists (a one-line DI rebind for the compliance engine, zero seam change). **B6 is now DONE (2026-07-17)** (`voxera-crm/features/20260716-compliance-gate-engine`, full SDLC — the compliance gate engine binds the `CallDispatchPolicy` port and fronts every dispatch: calling-hours/DNC/consent/recording-consent, per-motion frames, fail-closed, PII-safe audit, zero service change). **B5 → B7 → B8 are now all CODE-COMPLETE (2026-07-18 .. 07-26), the differentiator + trust + ops-shell spine built end-to-end** (see the rows above + the v13 changelog): B5 (AI voice operator, ADR-0040, all 4 slices), B7 (trust & observability, ADR-0041, backend S1/S3/S4 + FE S2/S3/S4 on Relay `src/core`; Slice-5 live-call deferred), B8 (daily-ops shell + the deterministic never-LLM report engine, ADR-0042, all slices A-G). **Every one is code-complete/VERIFY-PENDING on the same two gates: (1) the OOM host can't boot `createTestApp` → the pglite backend e2e run on CI/an unconstrained host; (2) the FE + `schema.graphql`/`graphql:gen:schema` regen + the deferred `app.module`/`Router.tsx`/`main.tsx`/i18n wiring land POST-MERGE.** All merge-safe (no conflict-file edits). **Next ready = B9** (Context Copilot + Configurator) — its prerequisite **TD-082 is in-flight** (owner chose 2026-07-26 to honor ADR-0006: migrate `@rjsf`→`@jsonforms` + build the Mantine renderer set; `voxera-crm/features/20260726-forms-jsonforms-migration`). ⚠️ **`rm/agentic` STILL CONFLICTS with `main`** (a parallel `#237` twilio-webhook/BullMQ-removal line — owner is doing that merge; read-only reconciliation plan delivered). Registry ticks v5-v12 fast-forwarded to `voxera-command` main 2026-07-20; ADR-0037..0042 on `voxera-os` main. B6 harvested; B5/B7/B8 pending verify→harvest.

## Track B — Website motion (`voxera-website`)

| Order | Item | Horizon | Depends on | Status | Workflow | Spec |
|---|---|---|---|---|---|---|
| 1 | **FEAT-001** Vertical landing-page templates (Astro) | Now | — | `SPEC-DRAFT` | **WEB-BUILD** (spec is `draft` → approve/refine first) | `features/FEAT-001-vertical-landing-templates-astro/spec.md` |
| 2 | **FEAT-002** `/real-estate` landing page | Now | FEAT-001 | `SPEC-DRAFT` | **WEB-BUILD** | `features/FEAT-002-real-estate-landing-page/spec.md` |
| 3 | `/home-services` landing + sub-vertical chips | Next | FEAT-002 | `NOT-STARTED` | **WEB-DESIGN** → WEB-BUILD | TBD |
| 4 | `/insurance` landing page | Next | spine B6 (TCPA) + IMO talks | `BLOCKED` | **WEB-DESIGN** → WEB-BUILD | TBD |
| 5 | "Get called by the AI now" demo backend (crm) | Next | B5, B6 | `BLOCKED` | **CRM-BUILD** | TBD |
| 6 | TCPA cornerstone resource page | Later | — | `NOT-STARTED` | **WEB-DESIGN** → WEB-BUILD | TBD |
| 7 | `/compare?competitor=X` DTR page | Later | paid-search live | `NOT-STARTED` | **WEB-DESIGN** → WEB-BUILD | TBD |
| 8 | Per-vertical activation email tweaks (RE/HS/Ins) | Later | — | `NOT-STARTED` | **WEB-DESIGN** → WEB-BUILD | TBD |

**Next ready on this track:** FEAT-001 (approve the draft spec, then **WEB-BUILD**). FEAT-002 unblocks on FEAT-001 DONE.

## Not roadmap bets (tracked for completeness)

- `voxera-crm/features/20260329-callback-auto-dialer` — Power Dialer callback auto-load — `IN-SDLC:VERIFYING`. Pre-dates the B-bets; finish via **CRM-RESUME** (`/verify-feature`) but it is **not** a roadmap line.

## Maintenance

- This registry is **derived state, kept by hand** — update a row's **Status** the moment a run finishes, and tick the matching `roadmap.md` checkbox in the same change. Bump `version` + add a Changelog line per [`docs.md`](../../.claude/rules/docs.md).
- When a CRM bet gets its feature folder, replace its **Feature folder** `TBD` with the real `features/<id>` path.
- Pairs with [`priorities-coach`](../../.claude/skills/priorities-coach/SKILL.md) (what to do next, by strategy) — this registry is *how* to execute it.

## Changelog
- 2026-07-28 v19: **B9 slice-2 (GAP-08 — the Context Copilot adaptive interview) BUILD CODE-COMPLETE → VERIFYING.** GAP-08 is the config-not-rebuild INPUT front door: a bounded conversational agent interviews the onboarding operator and writes the 11-section `BusinessContextVersion` live, replacing slice-1's plain form and handing to slice-1's Configurator (`requestWorkspaceDerivation`). Full SDLC on `rm/agentic` (`voxera-crm/features/20260728-context-copilot-interview`: spec → **7-lens design-review** [ai-agent LOAD-BEARING; the source-verified **NO-migration proof on 4 legs** — the B3 runtime is stateless-per-invocation, `AgentRun` has no conversation column, B5 persists only a pointer, and a durable transcript would invert the INV-13 PII invariant → the interview is **STATELESS-PER-TURN** with a caller-supplied transcript, re-derived + re-validated server-side] → test-cases → 5-slice plan → 5 build slices, each maintainer-verified): `d20f1967` the pure gate (`ContextDraftParser` fail-closed closed-keys + `InterviewCoverage` + **EXTRACT the single-sourced `isSectionValuePresent`** shared with `SectionedDocument.parse` → coverage⟺parse can never structurally disagree; 49 on-host) · `3921cf03` the write gate (`ContextCopilotService.complete` = server re-parse [the client's completeness flag is **ADVISORY**] + coverage gate + `appendVersion` under the human [INV-11 + PII + account-conditioned CASL] = the advisory **double-gate**; 7/7 on-host service-unit) · `affa4b0e` the interview turn (`advanceTurn` + `CONTEXT_COPILOT_CONFIG` [empty `toolNames:[]` = the strongest injection posture] + **`wrapUntrusted` EXTRACTED** to a shared domain helper [single-sourcing the security delimiter; the runtime's 3 sites refactored, behavior byte-identical] + the sanitizing `InterviewPromptAssembler` [drops injected system/tool transcript entries; the code-owned role card is the only system message] + the injection matrix; **advanceTurn NEVER appends** — the LLM is structurally OUTSIDE the durable write; completion is the deterministic coverage gate, never the model's self-report; 53/53 on-host FakeLlm) · `379c42b4` the GraphQL surface (`sendInterviewMessage` [capability-gate → advanceTurn, not a union] + `completeInterview` [deny-as-data, reusing the `setBusinessContext` union byte-for-byte via `run()`+`mapResultToUnion`]; **HANDOFF-1** no second derive surface; 12/12 resolver-unit) · `11f76c68` the FE chat (extend `BusinessContextScene`: `ContextInterviewRegion` mutation-driven + **client-custody `useReducer`** [transcript/draft NEVER Redux/URL/store] + 4 graph-blind Primitives + a READ-ONLY preview + the **REUSED** `requestWorkspaceDerivation` + routed `pilot/configurator` + VOX-FE-009 served + Enter-to-send/ARIA-live a11y; the form demoted byte-stable; 20 on-host / FE suite 1290). **NO migration all 5 (`gate:drift` PASS); the LLM structurally outside the write; agent⊆human; core→app 0, isolation 67 held; single derive path.** Reviewed baselines: app-tree 451→452 (the 1 resolver, TD-056), jscpd 380→381 (the deny-as-data `run()`/`sharedErrorRef` mirror — extracting it across the 5 schema classes = a pay-down-debt TD). Every AC (1-6) has on-host green evidence (the AC→evidence map + the `[CI]`-pending DONE gate in `features/20260728-context-copilot-interview/handoff.md`). **DONE gated on the same [CI] e2e** (the pglite append-row + the wired authZ e2e + `backend:graphql:gen:schema` + `frontend:relay` — `[CI]`-pending on the OOM host, the B5/B8/forms/slice-1 precedent). Honest deferrals: INV-12 Copilot-provenance + turn cost-metering (advanceTurn un-metered ephemeral elicitation); the `configurator` i18n namespace registration (inline defaults, consistent with slice-1). Deferred to LATER B9 slices: agents/gates/reports derivation (GAP-09 breadth + GAP-43), presets/templates (GAP-35/37), re-interview. Row 10 slice-2 → `IN-SDLC:VERIFYING (5/5)`. All on `rm/agentic` (owner merges at project end). Memory: `b9-configurator-derivation`.
- 2026-07-27 v18: **B9 slice-1 BUILD COMPLETE — all 6 slices, VERIFYING.** The Configurator "config-not-rebuild" derivation core is built end-to-end on `rm/agentic` (spec → 7-lens design-review → test-cases → plan → 6 build slices, each maintainer-verified): `e9879fd3` the pure DSL gate (parser+normalizer+validator-port+adapter→`WorkflowDefinition.create()` I-1..I-7; 33 tests RED→GREEN on-host) · `e53b8c64` the by-value VO + capture (propose-not-apply BY CONSTRUCTION — `captureConfigProposal` only inserts a Pending approval via the shared `insertPendingApproval`, never `create()`/`defineWorkflow`; U-VO 17/17 on-host; NO column) · `c29a7370` the tool-less Configurator run + the INJECTION MATRIX (**GREEN on-host** — empty tool surface `toolNames:[]` at config AND call-site + the reused `GROUNDING_PRECEDENCE_RULE`; sibling-driver not fork; worst case a Pending proposal, never an apply) · `c149d2d4` the apply (the `define_workflow` executor case — agent⊆human TRIPLE-GATE [upstream null-deny + validator-port re-validate + `defineWorkflow` CASL] + human-edit-byte-exact via `confirmedConfig`; 8/8 adapter spec on-host) · `5c18fbec` the GraphQL surface + async enqueuer (`setBusinessContext` 11-String! INV-11 · `requestWorkspaceDerivation` async `DerivationRun` ack · `ProposalReview.workspaceConfig` PROJECTION from the existing `descriptor Json` [no column] · REUSED approve + `confirmedWorkspaceConfig` single-path; 29 tests on-host) · `caa3565f` the FE (extend the B7 `ProposalReviewScene` [config-branch, the single-write path byte-stable] + the `WorkspaceConfigProposal` editable stage diff + `BusinessContextScene`; `DerivationRunStatusRegion` polls-newest-config-proposal [the runRequestId↔agentRunId correlation; `proposalForRun` follow-up deferred]; VOX-FE-009 served; 37 tests + full-suite 1267 GREEN on-host). **NO migration all 6; `gate:drift` PASS; core→app 0; context-isolation 67 held; single approval path; the B7 single-write proposal byte-stable.** Baselines: app-tree 447→451 (reviewed app/agent seams), jscpd 376→379 (sanctioned AgentRun-transport mirrors). Backend safety verified ON-HOST (the injection matrix + the FakeLlm/injected-fake suites) + BY CONSTRUCTION (propose-not-apply + the apply triple-gate); the FE fully on-host. **DONE gated on the CI e2e run** — the pglite service cases (SVC-CAPTURE/APPLY, SAFE-PROPOSE-NOT-APPLY/APPROVE-APPLIES/NULL-PRINCIPAL/EDIT-BYTE-EXACT/TENANCY) + the GraphQL authZ e2e + `backend:graphql:gen:schema` are `[CI]`-pending on the OOM host (the B5/B8/forms precedent). Row 10 → `IN-SDLC:VERIFYING (build 6/6 complete)`. A git-concurrency incident (a parallel agent's `checkout` reverted regenerable Relay artifacts) was bounded + self-resolved (slice-6's final relay run) + maintainer-verified (type-check + 37 tests green). Deferred to LATER B9 slices: GAP-08 the adaptive-interview UI (its role slot `ContextCopilot` exists), agents/gates/reports derivation (GAP-09 breadth + GAP-43), presets/templates (GAP-35/37). All on `rm/agentic` (owner merges at project end). Memory: `b9-configurator-derivation`.
- 2026-07-27 v17: **removed B13 (Deep DACH compliance — Pflegekasse + Art. 9) from Track A** (owner direction). Dropped the bet row + the "B13 dead-last" sequencing clause; the CRM product spine is now **B0–B12**. Reconciled the frontmatter `version` (was stale at 12 while the changelog had already reached v16). The broader **DACH GTM motion** ([ADR-0009](../../decisions/ADR-0009-english-markets-parallel-motion.md)) is unaffected; the underlying gaps **GAP-30/GAP-32** are retained in the [gap-analysis](./gap-analysis-crm-to-product.md) backlog but de-scoped to no bet. Companion removals in [`roadmap.md`](./roadmap.md) v10 + gap-analysis v2.
- 2026-07-27 v16: **B9 slice-1 build — slice 1/6 (the pure DSL gate) DONE.** The Configurator's safety spine shipped (`voxera-crm` `e9879fd3`, on `rm/agentic`): `FunnelProposalParser` (pure — closed-schema+bounds validation, deterministic slug derivation, never model text) + `FunnelNormalizer` (pure — agent-owned `ProposedWorkflowConfigProps`) + the core/agent-owned `WorkflowConfigValidatorPort` (`Result<void,AppError>` — no pipeline type leaks) + the app-side `workflow-config-validator.adapter` (the firewall SEAM — the ONLY pipeline importer; maps agent props → pipeline `WorkflowDefinitionProps` + runs `WorkflowDefinition.create()` I-1..I-7). The chain guarantees a broken funnel can NEVER become a valid config. Maintainer-verified: the firewall holds (core/agent imports zero `core/pipeline`; `core→app 0`); 33 tests RED→GREEN on-host (U-PARSE/U-NORM golden/U-GATE-I1..I7 accept+reject-per-invariant/SAFE-INVALID-PROPOSAL-FAIL-CLOSED); `gate:drift` PASS (no schema change), `isolation 67`, `app-tree 447→448` (the reviewed app-side firewall-seam adapter, TD-083). Row 10 → `IN-SDLC:IMPLEMENTING (build slice 1/6 done)`. **Remaining build slices 2-6** (VO+capture, the tool-less run, the apply, GraphQL+authZ, FE) want a fresh session's memory headroom (`createTestApp` OOM-fails late in this long session). Design fully locked in the dossier's 6-slice cut. Memory: `b9-configurator-derivation`.
- 2026-07-27 v15: **B9 slice 1 DESIGN + PLAN COMPLETE → BUILD-READY; the pure DSL gate building.** With TD-082 cleared, B9 slice 1 (the "config-not-rebuild" derivation core) went through its full authoring SDLC in this window (`voxera-crm/features/20260727-configurator-workspace-derivation`): spec (`d0f8bafc`) → **7-lens design-review, ALL approved** (`c7949e70`; ddd/prisma/graphql/ai-agent/oo/react/tdd — erd→prisma, webhook/analytics skipped; reviewer subagents stall epic-wide → maintainer adversarial-reviewed each vs SOURCE, tdd-designer stalled → maintainer-authored) → test-cases + 6-slice plan → **PLAN_APPROVED** (`752300a3`). **The design-review's payoff = a source-verified reframing that shrinks slice 1 to mostly WIRING:** `BusinessContext` ALREADY EXISTS as `BusinessContextVersion` (an 11-section `SectionedDocument`, INV-11 STRICT) + the `Configurator` role is an ADR-0004 fixed-7 slot (ContextCopilot is a SEPARATE slot = GAP-08, deferred) + **NO migration** (the derived config rides the existing `ProposedActionApproval.descriptor Json` + `toolName`). **OQ-1 = HYBRID:** a dedicated single-turn **TOOL-LESS** Configurator AgentRun (a sibling-driver reusing B3, the B5 stance) — LLM proposes a funnel from the grounding-compiled BusinessContext → deterministic `FunnelNormalizer` → **`WorkflowDefinition.create()` (I-1..I-7) is the DSL-valid gate** (a broken funnel can't be constructed) → a Pending B7 proposal → human-approved apply via B4's `defineWorkflow`. LOCKED adjudications: OQ-4 by-value VO on the existing `descriptor Json` (no column); the `WorkflowConfigValidatorPort` (not a direct pipeline import — MONO-006, the gate is necessary-not-sufficient); OQ-R1 edit label/order/terminal/disposition-only; async poll. **SAFETY BY CONSTRUCTION:** empty tool surface (`toolNames:[]`) + the coded `GROUNDING_PRECEDENCE_RULE` + agent⊆human + propose-not-apply → a poisoned BusinessContext escapes nothing. Reuses B4/B3(sibling)/B7(single approval path)/B0 — no second path, no god service, no migration. **Slice 1 of 6 (the pure DSL gate: parser+normalizer+validator-port) building now (on-host, FakeLlm+pure units);** slices 2-6 (VO+capture, the run, the apply, GraphQL+authZ, FE) want a fresh session's memory headroom (createTestApp OOM-fails late in this long session; the pure/service units run on-host, e2e are `[CI]`-pending regardless). Registry row 10 → `IN-SDLC:PLAN_APPROVED (slice 1 building)`. All on `rm/agentic` (owner merges at project end). Memory: `b9-configurator-derivation`.
- 2026-07-27 v14: **TD-082 (the B9 unblock) CODE-COMPLETE + B9 OPENED (slice 1).** The `@rjsf`→`@jsonforms` forms migration was built end-to-end on `rm/agentic` this window (`voxera-crm/features/20260726-forms-jsonforms-migration`, VERIFYING): full design-review (5 lenses, no new ADR — honors ADR-0006) → 8 commits (`679be8c0` slice-1 go/no-go gate GREEN [@jsonforms proven byte-identical to @rjsf via a closed-loop committed-golden oracle, OQ-1=Branch-B build-on-core] · `93a9d4b6` slice-2 backend config-as-code `formSchema` in `core/pipeline`, single-source insurer enum, TD-056 placement · `952b0ca9` the ONE `ContractTaskFormRegion` replacing the 2 ~1,000-line god components + full golden suite · `653a100b` side-effect hooks [resource-sync via a new Households barrel = firewall fix + pdf-lib dynamic SPLIT] · `166b0108` sign-flow UI + Region composition [R3 graph-blind orchestrator] · `3e27bb18` **consumer-flip** [Region LIVE in all 3 branch sites via a hook-free View/Live dispatcher; NewContract.page replaces the 2 wrapper pages] · `4a718d1e` **slice-6 @rjsf teardown** [relocated shared PDF utils→`contracts/pdf/`, DELETED the 6 god files −7,451 lines, UNINSTALLED @rjsf: zero imports in src + package.json/lock] · `7d7b7057` VERIFYING). **AC-2 (@rjsf gone) + AC-7 (PP-003 retired — one Region both products; jscpd 406→376, net 391→376 = duplication NET-REDUCED) MET**; the golden suite SURVIVES the @rjsf uninstall (the committed `*.golden.json` are the @rjsf-independent oracle). 7/10 ACs met; AC-4 (FE useFormSchema fetch) + AC-8 (typed docs) are **codegen-gated** (backend:graphql:gen:* OOMs on the dev host) → post-merge/CI; AC-3 (5 unused renderers) design-deferred to B9-incremental. **With TD-082 cleared, B9 is now OPEN** — slice 1 (the config-not-rebuild derivation core: `BusinessContext` model + the Configurator deriving a linear-funnel Workflow config, PROPOSED via the B7 primitive, agent⊆human; GAP-09 core + GAP-36; reuses B4/B3/B7/B0) → `voxera-crm/features/20260727-configurator-workspace-derivation` (DRAFT; OQ-1 derivation-mechanism [deterministic/LLM-agent/hybrid] = the load-bearing fork; next = SPEC_APPROVED + a large ~all-lens design-review). Deferred to later B9 slices: the adaptive-interview UI (GAP-08), agents/gates/reports derivation, presets/templates (GAP-35/37/43). All on `rm/agentic` (owner merges at project end). B5/B7/B8 + TD-082 all pending the same verify→DONE gate (the merge + a CI codegen/e2e run).
- 2026-07-26 v13: **B5 → B7 → B8 all CODE-COMPLETE (verify-pending) + B9 prerequisite TD-082 set to @jsonforms.** The differentiator + trust + ops-shell spine was built end-to-end across this window; the registry rows were stale at `NOT-STARTED` and are now corrected. **B5** (AI outbound voice operator, `voxera-crm/features/20260717-ai-voice-operator`) — 9-lens design-review + [ADR-0040], all 4 slices committed (`68689966`/`cb9ce363`/`fc99a536`/`1244a338`): server-originate through the B6 gate, Ultravox session + inbound webhooks (Svix-HMAC fail-closed, tenant-from-matched-row, no-PII, disclosure-before-Connect), the safety-critical VoiceToolCallExecutor (every tool-call SERVER-SIDE under the re-derived delegating human, agent⊆human null→fail-closed, structural injection defense, proposal-inline-not-terminated), DE voice + no-double-count cost. **B7** (trust & observability, `.../20260720-trust-observability-surface`) — 7-lens design + [ADR-0041] (first GraphQL subscription infra); backend S1/S3/S4 (reasoning + the ⭐honest-flag OQ-AI-2 guard; proposal + the VERBATIM reused approve/reject with deny-as-data + structural no-repoint; the no-double-count CausalTimelineComposer + autonomy) + FE S2/S3/S4 on Relay `src/core` (temp-schema-validated; the VOX-FE-009 CoreAbilityContext seam; **caught+fixed a write-path injection/corruption bug** — the diff pre-filled confirmedValues from a redacted preview); Slice-5 live-call subscription deferred (B5-gated + graphql-ws breakpoint). **B8** (daily-ops shell + report engine, `.../20260721-daily-ops-shell-report-engine`) — 7-lens design + [ADR-0042] (new `core/analytics` deterministic core-native context; compute-on-read no read-model; numbers via a single-source metric registry, **NEVER LLM** — import-graph-boundary-test-enforced); all slices A-G (`667f366e`/`9585543e`/`82c0f20c`/`d1f04e3b`/`f2676bc2`/`565c7a0c`): the engine core (⭐unpriced≠$0 mapper, ⭐no-LLM boundary), 2 cost groupBy methods (resolved a real host-TZ determinism bug → double AT-TIME-ZONE), the funnel adapter + **PipelineEvent index migration** (REPLACE, user-approved) + OQ-R1 default, the **AnalyticsReport ADD-VALUE migration** (55P04-safe) + seeder, the GraphQL read surface (3 typed queries + Measure union structurally never-$0, fail-closed-on-underlying; fixed a latent CASL-Subjects type gap), the FE ops-shell + funnel/cost dashboard (35/35 tests, MeasureValue never-$0, ConversionRate null→"—", GAP-48 not-yet-available). Both B8 migration breakpoints user-approved+cleared. **Every one is code-complete/VERIFY-PENDING on the same two gates:** (1) the OOM dev host can't boot `createTestApp` → all pglite backend e2e are CI-pending (confirmed hook-timeout at beforeAll, NEVER an assertion — the eval-composer/B5 precedent) → run on CI/an unconstrained host; (2) the FE `schema.graphql`/`graphql:gen:schema`/`frontend:relay` regen + the deferred `app.module`/`Router.tsx`/`main.tsx`/i18n wiring land POST-MERGE (all built merge-safe — no conflict-file edits — because `rm/agentic` STILL CONFLICTS with `main`'s parallel `#237` twilio-webhook/BullMQ-removal line, which the owner is merging; a read-only reconciliation plan was delivered). **The reusable technique that unblocked all FE on the OOM host:** the relay-compiler is a STANDALONE binary that runs on-host, so a hand-authored temp `schema.graphql` augmentation (mirroring the committed backend objectRefs, fields inlined into base `type Query` not `extend`, `Date` scalar) validates + typechecks + tests the Relay Scenes on-host; the temp schema is reverted (CI regenerates the real one). **Next ready = B9** (Context Copilot + Configurator) — prerequisite **TD-082 in-flight**: owner chose 2026-07-26 to HONOR ADR-0006 (migrate the 4 `@rjsf` product forms → `@jsonforms` + build the Mantine renderer set + server-owned schema, retiring PP-003/TD-088/TD-094) → `voxera-crm/features/20260726-forms-jsonforms-migration`. Follow-up TDs filed: TD-B7-TIMELINE-CHIP (expose agentRunId on ReasoningDecision), TD-B8-SEEDER-DEDUP (4th permission-seeder mirror → extract a shared helper), TD-B7-REASONING-FLAGS (B3 instrumentation for inference/contradiction markers). Registry ticks v5-v12 fast-forwarded to `voxera-command` main 2026-07-20; ADR-0037..0042 on `voxera-os` main. B5/B7/B8 pending verify→harvest.
- 2026-07-17 v12: **B6 (Compliance gate engine + per-motion frames) → DONE — B5 is next ready.** Shipped `voxera-crm/features/20260716-compliance-gate-engine` through the FULL gate-aware SDLC (spec → 6-lens design-review ddd/erd/prisma/graphql/oo/tdd, each a maintainer-adversarial-reviewed designer pass [reviewer subagents stall epic-wide] → [ADR-0038](../../../voxera-crm/decisions/ADR-0038-compliance-bounded-context-and-config-as-code-motion-presets.md) + [ADR-0039](../../../voxera-crm/decisions/ADR-0039-recording-consent-carried-directive-not-a-dispatch-gate.md) accepted+registered → tests → plan → 7 slices A-G test-first, CRM commits `50a25c7b`(A)·`bc90f2ff`(B)·`975c5e56`(C-E)·`9dec6deb`(F)·`1eccf10a`(G)·`030ec973`(DONE)). Binds the TD-081 `CallDispatchPolicyToken` (was `AllowAllCallDispatchPolicy`) to a **`ComplianceCallDispatchPolicy`** in a **new `core/compliance` bounded context** (the 2nd core-native after `agent`; ADR-0038): calling-hours + DNC + consent **deny-rules** (3 homogeneous `ComplianceRule`s behind one interface — OCP for future velocity/AMD rules) + **recording-consent carry** (never denies — a directive on `DispatchDecision.allowed`, reusing the ADR-0035 `decideRecording`; ADR-0039), all selected by a per-account **compliance frame** encoding the GTM **motion** (DACH UWG/GDPR ⇄ US TCPA/CMS-state) via **config-as-code legal presets** (code constants, ADR-0035 legal-list precedent) that the account tunes but is **floor-clamped** from disabling a mandatory rule. **The load-bearing invariants (all evidence-verified):** (1) **AC-1 zero service change** — `git diff` proves `call-dispatch.service.ts` was NEVER edited across all 7 slices (the frozen chokepoint; the token rebind lives in the @Global `ComplianceModule`, communication drops its local binding — NestJS local-provider-wins, fail-SAFE boot); (2) **the fail-OPEN trap closed structurally** — the dispatch-path reads (frame/DNC/consent) are RAW account-scoped (never `accessibleBy(initiator)`), so a low-priv caller can't silently bypass the gate (`TC-DNC-FAILOPEN` mutation-kill; `accessibleBy` grep-proven to live only in the 3 management services); (3) **deny-as-data + fail-closed** (a deny is the typed `DispatchDecision.denied{ruleId}`, any resolution/rule throw ⇒ deny, never allow); (4) **PII-safe audit** (every decision audited via the reused `AuthorizationAuditPort`, callee referenced by `contactId`/keyed token — never the raw E.164, B0 invariant). Plus a CASL-gated management surface (6 mutations + 4 queries, payload-error unions, 3-way authZ matrix) + 3 new `ApplicationResource` subjects. **2 migrations human-approved at the alwaysBreakOn** (additive/expand-only/55P04-safe: brand-new `CREATE TYPE` + the `ApplicationResource ADD VALUE ×3` **never consumed in-migration**, runtime-seeded — CONTROL-test-proven). **~1377 backend tests green run BY ZONE** (the full suite exceeds the harness duration cap, the B4-s3 pattern); `gate:ratchet`+`gate:drift`+`no-sdl-change` PASS (`core→app` 0, `context-isolation` 67, `app-tree` 424→429 reviewed for the seam adapter + Pothos schemas, `graphql`/`jscpd` reviewed bumps). Surfaced follow-ups: mandate a dedicated `COMPLIANCE_AUDIT_TOKEN_SECRET` in prod (currently falls back to the dispatch key); surface a machine-readable deny `ruleId` through the mutation once an AC-1-clean seam exists (IMPL-1, kept internal to preserve AC-1). **Next ready = B5** (AI voice operator — deps B3/B4/B6 all satisfied, the gate now fronts every live call). ⚠️ Registry ticks **v5-v12 + all CRM B-work UNMERGED** (`roadmap/b3-oq5-done` + `rm/agentic`); the B6 folder is **not yet harvested**.
- 2026-07-15 v11: **TD-081 (the B6 outbound-dispatch seam) → DONE — B6 precursor cleared, B6 is next ready.** Shipped `voxera-crm/features/20260714-call-dispatch-seam` (DONE; CRM commits `75ea04d9`..`59d79189`) through the FULL gate-aware SDLC (spec → 7-lens design-review ddd/erd/prisma/graphql/webhook/oo/tdd → [ADR-0037](../../../voxera-crm/decisions/ADR-0037-outbound-call-dispatch-authorization-mechanism.md) accepted+registered → tests → plan → 7 slices A-G test-first). The single backend `CallDispatchService` chokepoint every outbound-call initiation now traverses — closing the browser-initiated "dial anything" hole so the B6 compliance-gate engine has a place to enforce. **The load-bearing, source-verified design finding:** the provider voice token is target-agnostic and the REAL dial chokepoint is the `startTwilioCall` webhook (it dialled any client `To` "regardless of the decision") → OQ-1 = **(b) a stateless signed dispatch grant verified AT THE DIAL WEBHOOK** (fail-closed `<Reject/>`, caller-ID de-spoofed; **(c) server-originate** = B5 end-state). Built: the append-only `CallDispatch` record (additive/55P04-safe migration + column-guard trigger + PRE-SWEEP) · the grant crypto (constant-time, no raw target, injected clock, isolated key) · the audited service (CASL `create Voice` outside the tx, AMB-1 no-record-on-denial, one-tx atomicity) · the `requestCallDispatch` union mutation (DENY-as-data, no new `ApplicationResource`) · the dial-webhook INV-7 gate · the FE Dialer re-route (behind the NFR-4 flag). **The `CallDispatchPolicy` `@Global` port IS the B6 bind-point** — a one-line DI rebind, zero service change (AC-5). ~947 backend + 1136 frontend tests green (0 related failures; 2 pre-existing date-dependent PDF fails); `gate:ratchet`+`gate:drift`+`no-sdl-change` PASS (core→app 0, context-isolation 67, app-tree +0). Governance folded into the schema slice (LSN-052). Marked TD-081 resolved (voxera-crm tech-debt v43) + the B6 row (precursor cleared). **Next ready = B6** (deps B4 ✓ + TD-081 ✓) → B5. ⚠️ Registry ticks **v5-v11 + all CRM B-work UNMERGED** on `roadmap/b3-oq5-done` + `rm/agentic` — awaiting owner merge to `main`.
- 2026-07-14 v10: **B4 (Typed Workflow DSL + primitives + write-path) → DONE** + next move set to **TD-081 → B6**. B4's entire roadmap scope — GAP-04 (typed `Workflow`+`Stage` DSL), GAP-33 (primitives registry), GAP-52 (agent-driven transitions), GAP-34 (time-triggers) — is closed by slices 1-3 (v7/v8/v9), so the bet is complete; ticked the `roadmap.md` B4 checkbox + set the registry row `DONE`. **Scope correction:** the "forms/reports/custom-objects/non-linear-kinds" that earlier notes attached to B4 are NOT B4 GAPs — reports → B8 (GAP-42/44), custom-objects → B11 (GAP-03), forms → B9 (+ the TD-082 forms-ADR), and non-linear `workflow_kind` (dag/parallel) is a forward-compat extension the DSL was designed for but that no current GAP requires; none belong to B4. **Next ready (owner-chosen 2026-07-14):** **TD-081 → B6.** With B4 done, the next spine bets by Order are B6 (compliance gate, deps B4 ✓) and B9 (Configurator, deps B1/B3/B4 ✓), but each is gated on a prerequisite: **TD-081** (no backend outbound-dispatch seam — dialing is browser-initiated, so the ADR-0023 compliance gate has nothing to front; remediation = a `CallDispatchService` chokepoint in `core/communication` every initiation traverses) blocks B6, and **TD-082** (the ADR-0006 `@jsonforms` vs shipped `@rjsf` contradiction) blocks B9. Owner chose to pay down **TD-081** first (a `build-feature`-sized backend dispatch seam — HIGH, `communication` context) to open **B6 → B5** (the compliance-gate → AI-voice-operator differentiator path). B5 stays blocked on B6 (the gate must front every live AI call, ADR-0023). Website track unchanged (FEAT-001 ready).
- 2026-07-14 v9: **B4 slice 3 (workflow time-triggers, GAP-34) → DONE.** The first UNATTENDED driver of the slice-1 transition substrate — a stage-declared time-trigger fires a stage transition with no human and no agent in the loop, yet still under real CASL. On the CRM side (`voxera-crm/features/20260713-workflow-time-triggers`, commits `a47e6a6a`..`b8781693`): full gate-aware build-feature SDLC (spec → 6-lens design-review DDD/ERD/Prisma/webhook/OO/TDD, all converged → tests → plan → 8 slices A-H test-first). A stage carries a `StageTimeTrigger` (`afterMs` offset → `transition_to` a TERMINAL stage, define-time validated I-8/I-9/I-9b/I-10); on a transition INTO the stage the engine materializes one Pending `ScheduledTransition` per trigger **in the same tx** (`fireAt = enteredAt + afterMs`) and cancels the left stage's Pending jobs; an `@Interval` poller (**Design A tx-join**, version-guarded claim, batch-cap + single-flight — the `AgentRunReaper` precedent) fires due jobs through the slice-1 **`PrimitiveRegistry` → `StageTransitionService`** (UNCHANGED — no second write path) under a **per-account System Automation Principal** ([ADR-0036](../../../voxera-crm/decisions/ADR-0036-system-automation-principal.md); a real iam identity with a runtime-seeded `{accountId}` `modify Lead` grant → the fire is authorized, tenant-scoped, `PipelineEvent`-attributed, never bypassed; null ctx → fail-closed). **Schema:** 2 new models + 3 enums, an **additive/expand-only, 55P04-safe migration** (human-approved at the `database-migration` breakpoint; NO `ALTER TYPE ADD VALUE`, NO `Permission` INSERT — the grant is runtime-seeded), FK `stageTimeTriggerId` **ON DELETE SET NULL** (Correction 2 — a status-blind Restrict would abort a workflow re-version/teardown once a trigger had any historical job), and a raw partial-unique `(leadId, stageTimeTriggerId) WHERE status='Pending'` dedupe. **No `ApplicationResource` change → no SDL regen** (`requiresGraphqlGeneration=false`, `no-sdl-change` green). **~1826 tests across all zones green, 0 failures; `gate:ratchet` + `gate:drift` + `no-sdl-change` + `migration-additive` PASS** (`core→app` 0, `context-isolation` held **67**, `app-tree` 422→**424** — reviewed +2 for the iam adapter + the extracted `buildExecutionContext` helper, iam not yet core-migrated). Both dossier corrections proven by dedicated tests: the **separate-tx settle on a thrown fire** (LSN-048 — a naive in-tx settle leaves the job stuck Pending) and the **SetNull re-version** (a re-version with a live Pending + a historical Fired job succeeds, the Fired job's FK SetNulls). Two source-verified design refinements within the locked design: `moved`-detection via the in-tx `PipelineEvent` count delta (slice-1 unchanged), and authz-denial settling via the separate-tx path (durable-deny survives on the independent audit conn). Spawned a low-pri test-perf follow-up (the `AUTHZ-FORBIDDEN` pglite poison-tx ~5s artifact; prod uses a separate audit conn) + a `build()`-complexity harvest note. **B4 remaining:** forms/reports/custom-objects/non-linear-kinds — none dependency-blocked. Ticked the B4 row (stays IN-SDLC; B4 not fully done). ⚠️ Registry ticks **v5(OQ-5)/v6(TD-101)/v7(B4-s1)/v8(B4-s2)/v9(B4-s3) ALL UNMERGED** on branch `roadmap/b3-oq5-done` awaiting merge to `main`.
- 2026-07-13 v8: **B4 slice 2 (agent-driven stage transitions) → DONE.** The B3×B4 integration slice: an Operator agent PROPOSES a lead stage transition (`transition_lead` tool), a human APPROVES it, and slice-1's `StageTransitionService` executes **under the human** via B3 OQ-5's approval sink — closing GAP-52's remaining half (the agent-driven decision → `PipelineEvent` binding slice-1 deferred). On the CRM side (`voxera-crm/features/20260713-agent-driven-transitions`, commits `f3a9c623`..`46996745`): 3-lens design-review (ai-agent load-bearing / OO / TDD — all converged, adversarial reviews maintainer-performed as the reviewer subagents stalled; ddd/erd/prisma/graphql/webhook/analytics/react scoped out — **no schema change, `requiresPrismaMigration=false`**, `transition_lead` is a new row-shape not a new model, no new `ApplicationResource`) → 18 TCs → 6 dependency-ordered slices test-first (A `buildTransitionRequestFromConfirmed` closed-enum guard → B `PROPOSED_WRITE_PROJECTIONS['transition_lead']` → C executor `RegisteredWrite` + `existsUnderHuman('Lead')` → D approve path [no new code] → E idempotency [tx-join, no migration] → F the SIDE_EFFECTING tool on the Operator surface). **Full `backend:test` 224 suites / 1920 tests green; `gate:ratchet` + `gate:drift` PASS; `core→app=0` (firewall held); NO new `app/` file (app-tree 422 unchanged); backend:lint clean.** Load-bearing safety, all test-confirmed: **injection containment** (the executed move is built ONLY from the captured `leadId` + the human's `confirmedValues` — the model's preview never reaches the executor; a proposed advance vs a confirmed exit executes the EXIT), **agent ⊆ human** (null re-derived context → fail-closed, no fallback identity), **execute-once** (the transition's `@Transactional Propagation.Required` JOINS the approve tx → a version-race loser rolls back atomically → exactly one `PipelineEvent`; no idempotency column), **durable-deny-on-denial-only** (a real `modify Lead` denial writes a deny row; a version-conflict does not). The full-suite run surfaced + fixed a **pre-existing** SDL drift (`ApplicationResource` gained `ProposedActionApproval` [OQ-5] + `Workflow` [slice-1] in the zmodel but the root `schema.graphql` was never regenerated — B4-s2 added no `ApplicationResource` value) → spawned **TD-108** (add a fast SDL-regen-diff gate) + **TD-109** (extract `existsUnderHuman`'s `RESOURCE_REVALIDATORS` map at the 3rd type); **LSN-050** (SDL regen must ship with its slice; `no-sdl-change` only bites in the full suite) + **LSN-051** (a SIDE_EFFECTING tool's `execute` is the reachable `humanPresent && approved` path, not dead code). **B4 remaining slices:** slice 3 time-triggers (GAP-34), + forms/reports/custom-objects/non-linear-kinds — none dependency-blocked. Ticked the B4 row.
- 2026-07-13 v7: **B4 slice 1 (Typed Workflow DSL — executable substrate) → DONE.** The next dependency-unblocked bet opened + shipped its first vertical slice through the FULL gate-aware build-feature SDLC (spec → SPEC_APPROVED → 5-lens design-review DDD/ERD/Prisma/OO/TDD, all converged → DESIGN_APPROVED → test-cases → TESTS_APPROVED → plan → PLAN_APPROVED → implement test-first → DONE). On the CRM side (`voxera-crm/features/20260711-workflow-dsl-linear-funnel`, commits through `4496ae63`): replaces the untyped `Pipeline.definition Json` (never parsed; the real transition path branched **hardcoded stage arrays on a literal account id**) with **normalized `Workflow`+`Stage` models** (user chose OQ-1 = Option B) — a typed `linear_funnel` define/version write-path (CASL, `Workflow.version` optimistic lock, full-set-replace), a validated stage-transition write-path (single-source `Lead→LeadPipeline(isActive)→Workflow` resolve, T-6 fail-closed, atomic `Lead.stage`+`PipelineEvent` in one tx via the **resource-owned `LeadStageDenormPort`** firewall seam — `context-isolation` held **67**), a `PipelineAccountTeardownPort` (the Restrict-FK teardown companion), and a one-member primitives registry. **62 core/pipeline + 70 account + 75 authz tests green; `gate:ratchet` + `gate:drift` PASS; 0 lint errors; coverage 80-100% on the load-bearing files.** The human-approved additive migration cleared the `database-migration` breakpoint. Legacy `LeadWorkflowServiceImpl` **parallel-served** (OQ-2, strangler IRON-LAW) → spawned **TD-106** (strangle it after a Pipeline→Workflow backfill) + **TD-107** (route the repository's resource-owned Lead READ through a port); LSN-049. **B4 remaining slices:** slice 2 agent-driven transitions (rides B3 `ProposedAction`), slice 3 time-triggers (GAP-34), + forms/reports/custom-objects/non-linear-kinds — none dependency-blocked. Ticked the B4 row.
- 2026-07-11 v6: **B3 TD-101 (turn-level LLM retry) → DONE.** A provider transient (Anthropic 500/429/timeout) no longer terminally fails an automation run. On the CRM side (commit `ef30a8ab`): ai-agent design pass (designer → adversarial reviewer, converged with 4 fixes) → test-first. `runChatTurn` retries a RETRYABLE `generate()` failure (cap 2, full-jitter backoff, budget-aware via `remainingWallClockMs()`) before the unchanged `LLM_FAILURE` fallthrough; a core-owned typed `LlmGenerationError { retryable, category }` with the provider status→retryable mapping isolated in the Mastra infra adapter (VOX-CORE-001 clean); idempotency-safe (`runChatTurn` executes no tools/writes, only the successful attempt's usage is ledgered). 19 core/agent/runtime suites / 149 tests green; `gate:ratchet` PASS (no baseline bump). Spawned **TD-105** (low — circuit-breaker + `Retry-After` for provider-outage retry storms, gated on B5/B6 concurrency). **B3's remaining work is now the 6 other agent roles**, which are gated on their downstream consuming bets (B9/B12) — so **B4 (Typed Workflow DSL) is the next dependency-unblocked fresh bet** on this track.
- 2026-07-10 v5: **B3 OQ-5 approval sink → DONE.** The propose-then-handoff loop is closed — the B3 async entrypoint's ProposedActions are now an actionable human surface. Full SDLC on the CRM side (`voxera-crm/features/20260707-agent-approval-sink`): 6-lens design (DDD/ERD/Prisma/AI-agent/OO/TDD, adversarially reviewed) → 15 vertical slices S1..S15 test-first (commits `00e1943f`..`a2d222b0`, incl. the human-approved additive migration at S3) → verified DONE: 538 core/agent tests / 68 suites green, branch coverage 68.12% (≥ the 80/68/80/80 per-path floor), `gate:ratchet` PASS (firewall `core-import-boundary` 0, `service-authz` 0, `authz-matrix` 0, `no-pii-in-the-clear` 0; `app-tree` 417→420 for the 3 `@Global` firewall-seam adapters — executor + audit; jscpd within baseline), 0 lint errors. Safety core independently verified: injection defense (the model's proposed args NEVER reach the executor — only human-confirmed values), agent ⊆ human execution under the RE-DERIVED ability with null→fail-closed (no fallback identity), two-path durable audit (allow atomic-in-tx / deny durable-across-rollback). Spawned **TD-104** (med — OQ-4 lazy expiry never PERSISTS: the `Expired` write rolls back with the `@Transactional` `Result.err` re-throw; NOT a safety hole — the stale proposal is refused + never approved; needs a tx-structure fix) + **TD-103** (low — seeder over-grant, inert). Ticked B3 row + Next-ready. B3 remaining: TD-101 (turn-level LLM retry) → the other 6 roles.
- 2026-07-07 v4: **B1 → fully DONE.** TD-086 (slices 4-5: BusinessContext AC-5 + AgentActionCost AC-6) implemented test-first (5 slices S-A schema/migration → S-E adapters + 5 P15 runtime exceptions; commits `84fe45a7`..`1ee4db01`) and verified DONE (evidence-based: core/agent 43 suites / 381 tests green, 0 skipped; `gate:drift` PASS; `gate:ratchet` PASS — agent-context core-import-boundary/ddd-coverage/service-authz/authz-matrix/no-pii all 0; context-isolation 68→67; app-tree at-baseline). Un-stubs B3's `BusinessContextPort`/`AgentCostLedgerPort` — **B3's grounding + cost adapters are now LIVE**. Only AC-7 (inspect chip) still deferred → B7 (frontend, ADR-0034 Relay). Spawned TD-101 (turn-level LLM retry), TD-102 (DecisionRecord unbacked Restrict FKs), OQ-TDD-5 (seeder over-grant, INERT). Ticked roadmap.md B1 (Context+cost). Next ready → B3's remaining slices (OQ-5 approval sink → TD-101 → 6 roles). CRM feature folder awaits `harvest-feature` (then slims + archives).
- 2026-07-06 v3: **catch-up sync with the CRM repo** (the runs completed without ticking this registry — restored). **B1 → DONE** (2026-07-03, foundational spine: AutonomousAgent/AgentRun/DecisionRecord + outbox provenance, full SDLC, ADR-0028/0029; BusinessContext/AgentActionCost/inspect-chip deferred = TD-086, folder active). **B2 → DONE** (2026-07-04, harvested: consumer-group relay over a RANGE-partitioned outbox, ADR-0030/0031; TD-009 taxonomy settled via ADR-0032, per-context emit wiring mechanical). **B3 → IN-SDLC:IMPLEMENTING** (runtime substrate done 2026-07-06: Mastra + turn loop + delegating-principal authz + TD-100 hardening + reaper + async/BullMQ entrypoint; remaining: 6 roles, TD-086 adapters, OQ-5 approval sink, TD-101). Interleaved non-bet work recorded on the CRM side: P1 security sprint (TD-078/079/080 + ADR-0035 consent), strangler forcing-functions (app-tree + frontend gates, ADR-0033), Relay pivot (ADR-0034, TD-099 slice 1). Ticked roadmap.md B1/B2. Next ready recomputed → B3's remaining slices (TD-086 first).
- 2026-06-29 v2: **B0 → DONE** (verify-feature green: 1054 backend tests pass / 0 fail, gate:ratchet PASS with all four B0 gates floor-0, `schema.graphql` empty diff, all 22 ACs + 14 preconditions verified). Ticked `roadmap.md` B0 checkbox. Next move recomputed: HARVEST B0, then the P0 hardening slice (B1 prerequisite) before B1 opens.
- 2026-06-29 v1: initial — execution map for roadmap.md v8 (B0–B13 spine + website motion), workflow templates, two-track registry, selection algorithm for the `resume-roadmap` skill.
